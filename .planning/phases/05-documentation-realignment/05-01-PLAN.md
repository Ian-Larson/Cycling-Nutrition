---
phase: 05-documentation-realignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - PRODUCT.md
autonomous: true
requirements:
  - DOC-01

must_haves:
  truths:
    - "PRODUCT.md exists at the repo root and is tracked in git"
    - "PRODUCT.md no longer references the #547597 / #C9C6C5 / #EBEBEB palette"
    - "PRODUCT.md no longer references dark-mode parity"
    - "PRODUCT.md documents the orange #f8622e brand as the shipped identity"
    - "PRODUCT.md references the actual primitives directory src/components/ui/"
    - "PRODUCT.md references the actual @theme tokens (brand/shell/ink scales, IBM Plex Sans fonts)"
    - "PRODUCT.md describes the post-sweep IA — Fuel Plan, Garage, Account"
  artifacts:
    - path: "PRODUCT.md"
      provides: "Design context grounded in shipped reality"
      contains: "#f8622e"
  key_links:
    - from: "PRODUCT.md"
      to: "src/components/ui/"
      via: "Primitives section reference"
      pattern: "src/components/ui"
    - from: "PRODUCT.md"
      to: "src/index.css @theme block"
      via: "Tokens section reference"
      pattern: "@theme|--color-brand|--font-"
---

<objective>
Rewrite PRODUCT.md so it describes Domestique as it actually ships after Phases 1–4 — orange `#f8622e` brand, light-mode only, IBM Plex Sans typography, real primitives, real tokens, real IA — and commit the file (it is currently untracked). This closes DOC-01.

Purpose: Future contributors and future Claude planning sessions read PRODUCT.md as design context. The current file describes a different brand direction (`#547597` palette, dark-mode parity) that does not match the shipped product. Stale design context drives incorrect downstream decisions; correcting it lands the milestone's documentation realignment.

Output: A rewritten, tracked `PRODUCT.md` at the repo root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@PRODUCT.md
@src/index.css
@src/components/ui/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite PRODUCT.md to shipped reality and commit</name>
  <files>PRODUCT.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (DOC-01 verbatim contract)
    - PRODUCT.md (current 18-line file — confirm starting state)
    - src/index.css (lines 1-58 — @theme block: confirm brand/shell/ink/font tokens before referencing them)
    - src/components/ui/index.ts (confirm primitive export list before naming primitives)
    - .planning/phases/01-layout-tab-foundations/01-UI-SPEC.md (tone/voice reference for design contract prose)
    - .planning/phases/04-account-consolidation/04-02-SUMMARY.md (confirms /account is the consolidated 2-pane page — IA reference)
  </read_first>
  <action>
Use the Write tool to replace the entire contents of `PRODUCT.md` (currently untracked, 18 lines) with the verbatim text below. Do not paraphrase. Do not insert additional sections. Do not change heading levels.

```markdown
## Design Context

### Users
Primarily experienced self-coached cyclists using the app before a ride, often on mobile, to build a fast and reliable fueling plan without doing nutrition math by hand. They need to confirm kit readiness, set ride demands, and leave with precise numbers and followable instructions quickly.

### Brand Personality
Precise, efficient, focused. The product should feel dependable, clear, and fast under time pressure. Copy should be brief, concrete, and useful to cyclists who want numbers and actions without marketing language.

### Aesthetic Direction
Quiet utility-first interface, not SaaS. Brand identity is the warm orange `#f8622e` (`--color-brand-500`) used sparingly for active controls, primary CTAs, and focus rings. Surfaces sit on warm whites (`--color-shell-50`, `--color-shell-100`) over a cool ink-grey type scale (`--color-ink-50` through `--color-ink-900`). Typography is IBM Plex Sans across body, UI, and display; weights 400 / 500 / 600 / 700. Light mode only — no dark variants in this milestone. Avoid oversized headlines, dashboard theater, ornamental gradients, product-marketing chrome, and copy that restates visible information.

### Design Principles
1. Optimize for speed of use: reduce taps, reduce reading, reduce hesitation.
2. Prioritize numeric clarity: show the stats, sources, and actions cyclists need first.
3. Keep copy tight: no duplicated explanations, no slogans, no filler.
4. Design as a utility tool, not a SaaS dashboard or marketing surface.
5. Build hierarchy through spacing, grouping, and weight rather than oversized type.
6. Meet light-mode accessibility — sufficient contrast on text and borders, non-color-only state cues, and reduced-motion-safe interactions.

### Primitives
Hand-rolled UI components live in `src/components/ui/`: alert, badge, button, card, checkbox, collapsible, dialog, divided-row-list, icon-button, input, preset-buttons, segmented-control, select, spec-row, stepper, tabs, toast, toggle. New surfaces compose these — net-new primitives only when foundation work absolutely requires it.

### Tokens
All colors, fonts, and spacing live in the Tailwind v4 `@theme` block at `src/index.css`. Brand orange (`--color-brand-50` through `--color-brand-900`), shell warm-whites (`--color-shell-50` through `--color-shell-300`), ink cool-greys (`--color-ink-50` through `--color-ink-900`), and semantic success / warning / error scales are tokens — not hex literals. Fonts: `--font-sans`, `--font-body`, and `--font-display` all resolve to IBM Plex Sans.

### Information Architecture
Three primary tabs — Fuel Plan, Garage, Account.
- **Fuel Plan** (`/`) — Pack / Ride guide / Stats sub-tabs; the right rail shows a single "Fuel Inventory" panel.
- **Garage** (`/gear`) — Active setup / Service / History sub-tabs; the "Due Soon" service card appears only on Service. Inventory sub-route at `/gear/inventory`.
- **Account** (`/account`) — single 2-pane page: Athlete profile and fuel-target preferences on top; Sign-in, cloud-sync, and Strava connect-disconnect below. Legacy `/athlete` and `/settings` routes redirect to `/account[#preferences]`.
```

After writing, stage the file so it is tracked:

```bash
git add PRODUCT.md
```

Do not commit — the orchestrator handles the phase commit.
  </action>
  <verify>
    <automated>grep -c '#547597\|#C9C6C5\|#EBEBEB' PRODUCT.md | grep -qx 0 && grep -c '#f8622e' PRODUCT.md | awk '$1 >= 1 {exit 0} {exit 1}' && grep -ic 'dark.mode\|dark theme\|light and dark' PRODUCT.md | grep -qx 0 && grep -c 'src/components/ui' PRODUCT.md | awk '$1 >= 1 {exit 0} {exit 1}' && grep -c '@theme\|--color-brand\|--font-' PRODUCT.md | awk '$1 >= 1 {exit 0} {exit 1}' && grep -c 'Fuel Plan\|Garage\|Account' PRODUCT.md | awk '$1 >= 1 {exit 0} {exit 1}' && grep -c 'IBM Plex Sans' PRODUCT.md | awk '$1 >= 1 {exit 0} {exit 1}' && git ls-files --error-unmatch PRODUCT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '#547597\|#C9C6C5\|#EBEBEB' PRODUCT.md` returns `0` (stale palette removed)
    - `grep -c '#f8622e' PRODUCT.md` returns `>= 1` (shipped brand documented)
    - `grep -ic 'dark.mode\|dark theme\|light and dark' PRODUCT.md` returns `0` (dark-mode parity removed)
    - `grep -c 'src/components/ui' PRODUCT.md` returns `>= 1` (primitives reference present)
    - `grep -c '@theme\|--color-brand\|--font-' PRODUCT.md` returns `>= 1` (tokens reference present)
    - `grep -c 'Fuel Plan\|Garage\|Account' PRODUCT.md` returns `>= 1` (IA reference present)
    - `grep -c 'IBM Plex Sans' PRODUCT.md` returns `>= 1` (typography reference present)
    - `git ls-files --error-unmatch PRODUCT.md` exits `0` (file is now tracked)
  </acceptance_criteria>
  <done>
    PRODUCT.md contains the new content verbatim, all 8 grep gates above pass, the file is staged via `git add`, and `git status` reports it as a tracked staged file (no longer in the untracked list).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author intent → future contributors | Documentation prose is the only artifact crossing here; correctness governs downstream decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure (misleading) | PRODUCT.md "Aesthetic Direction" prose | mitigate | Replace stale palette tokens with verified `@theme` references; verify with grep gate that `#547597`/`#C9C6C5`/`#EBEBEB` are absent |
| T-05-02 | Tampering (drift) | PRODUCT.md "Design Principles" item 6 | mitigate | Rewrite principle 6 to drop dark-mode parity language; verify with grep that `dark mode`/`light and dark` are absent |
| T-05-03 | Repudiation (untracked file) | PRODUCT.md presence in git | mitigate | Stage with `git add PRODUCT.md`; verify with `git ls-files --error-unmatch PRODUCT.md` |
| T-05-04 | Information Disclosure | document content | accept | No secrets, credentials, or PII; pure design context prose |
</threat_model>

<verification>
- All 8 grep / git acceptance gates pass.
- `git status` shows `PRODUCT.md` under "Changes to be committed" (not "Untracked files").
- No source files (`src/**`) modified by this plan.
</verification>

<success_criteria>
DOC-01 satisfied: PRODUCT.md is committed (no longer untracked), contains no `#547597` / `C9C6C5` / `EBEBEB` palette references, contains no dark-mode parity language, documents the orange `#f8622e` brand, references the actual primitives directory, references the actual `@theme` tokens, and describes the actual post-sweep IA (Fuel Plan / Garage / Account).
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation-realignment/05-01-SUMMARY.md` per the GSD summary template.
</output>

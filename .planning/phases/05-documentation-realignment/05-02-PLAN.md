---
phase: 05-documentation-realignment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements:
  - DOC-02

must_haves:
  truths:
    - "CLAUDE.md's font reference reads IBM Plex Sans"
    - "The string Outfit no longer appears anywhere in CLAUDE.md"
    - "The string Source Sans 3 no longer appears anywhere in CLAUDE.md"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Project guidance with correct font reference"
      contains: "IBM Plex Sans"
  key_links:
    - from: "CLAUDE.md line 29"
      to: "src/index.css @theme --font-sans / --font-body / --font-display"
      via: "Tailwind v4 fonts bullet"
      pattern: "Tailwind CSS v4.*IBM Plex Sans"
---

<objective>
Replace CLAUDE.md's stale font reference on line 29 — `(Outfit, Source Sans 3)` — with the actual font in the `@theme` block — `(IBM Plex Sans)`. One-line correction. This closes DOC-02.

Purpose: CLAUDE.md is the first file every Claude Code session reads. Naming fonts that aren't in the codebase causes future contributors (human or AI) to import / reference Outfit and Source Sans 3 packages that are not part of the project. Aligning the bullet with `src/index.css` `@theme` removes that drift.

Output: CLAUDE.md line 29 corrected; `Outfit` and `Source Sans 3` no longer appear anywhere in the file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct font reference on CLAUDE.md line 29</name>
  <files>CLAUDE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (DOC-02 verbatim contract)
    - CLAUDE.md (confirm line 29 currently reads `- **Tailwind CSS v4** - Styling with custom fonts (Outfit, Source Sans 3)`)
    - src/index.css (lines 1-7 — confirm `--font-sans / --font-body / --font-display` all resolve to `"IBM Plex Sans"`)
  </read_first>
  <action>
Use the Edit tool on `CLAUDE.md` with this exact string replacement (column-0 alignment, no leading whitespace beyond what is shown):

old_string:
```
- **Tailwind CSS v4** - Styling with custom fonts (Outfit, Source Sans 3)
```

new_string:
```
- **Tailwind CSS v4** - Styling with custom fonts (IBM Plex Sans)
```

This is a single-occurrence, single-line replacement. No other lines in CLAUDE.md change.
  </action>
  <verify>
    <automated>grep -c 'Outfit' CLAUDE.md | grep -qx 0 && grep -c 'Source Sans 3' CLAUDE.md | grep -qx 0 && grep -c 'IBM Plex Sans' CLAUDE.md | grep -qx 1 && grep -nq '^- \*\*Tailwind CSS v4\*\* - Styling with custom fonts (IBM Plex Sans)$' CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Outfit' CLAUDE.md` returns `0`
    - `grep -c 'Source Sans 3' CLAUDE.md` returns `0`
    - `grep -c 'IBM Plex Sans' CLAUDE.md` returns `1` (exactly one occurrence — the corrected bullet)
    - The bullet `- **Tailwind CSS v4** - Styling with custom fonts (IBM Plex Sans)` appears verbatim in CLAUDE.md
    - No other lines in CLAUDE.md changed (confirm via `git diff --stat CLAUDE.md` shows `1 file changed, 1 insertion(+), 1 deletion(-)`)
  </acceptance_criteria>
  <done>
    CLAUDE.md line 29 reads `- **Tailwind CSS v4** - Styling with custom fonts (IBM Plex Sans)`, all 4 grep gates pass, and `git diff --stat CLAUDE.md` reports exactly one insertion and one deletion.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLAUDE.md → every future Claude Code session | This file is auto-loaded as project instructions; incorrect content propagates into every future planning and execution session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Information Disclosure (stale fact) | CLAUDE.md line 29 fonts bullet | mitigate | Replace `(Outfit, Source Sans 3)` with `(IBM Plex Sans)`; verify with grep gates that the stale strings are gone and the new string is present exactly once |
| T-05-06 | Tampering (over-edit) | CLAUDE.md other lines | mitigate | Single-occurrence Edit with exact old_string match; verify with `git diff --stat` that exactly one line is changed |
| T-05-07 | Information Disclosure | document content | accept | No secrets, credentials, or PII; pure project guidance prose |
</threat_model>

<verification>
- All 4 grep / diff acceptance gates pass.
- `git diff CLAUDE.md` shows exactly one removed line and one added line — both on line 29.
- No source files (`src/**`) modified by this plan.
</verification>

<success_criteria>
DOC-02 satisfied: CLAUDE.md's font reference reads `IBM Plex Sans`. The strings `Outfit` and `Source Sans 3` no longer appear in CLAUDE.md.
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation-realignment/05-02-SUMMARY.md` per the GSD summary template.
</output>

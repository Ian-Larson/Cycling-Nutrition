---
phase: 02-fuel-plan-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/planner/setup-card.tsx
autonomous: true
requirements:
  - FUEL-01
  - FUEL-02
must_haves:
  truths:
    - "The setup flow's bottles-collapsible header reads 'Available bottles' (not 'Bottles')."
    - "The setup flow's fuel-selection collapsible header reads 'Fuel selections' (not 'Fuel')."
    - "Bottle quantity is captured exactly once in the setup flow under the renamed label."
    - "No other label, prop, behavior, or style in setup-card.tsx changes — pure copy edit."
  artifacts:
    - path: "src/components/planner/setup-card.tsx"
      provides: "Setup flow with renamed bottles and fuel section labels"
      contains: "Available bottles"
  key_links:
    - from: "src/components/planner/setup-card.tsx"
      to: "src/pages/planner.tsx"
      via: "<SetupCard variant='embedded' .../> JSX invocation at line 618"
      pattern: "SetupCard"
---

<objective>
Rename the two collapsible section headers in the setup flow — "Bottles" → "Available bottles", "Fuel" → "Fuel selections" — to match the FUEL-01 and FUEL-02 contract from REQUIREMENTS.md.

Purpose: Phase 2 success criteria 2, 3 — bottle quantity is captured exactly once in setup under the label "Available bottles" (FUEL-01), and the fuel-selection label reads "Fuel selections" (FUEL-02).

Output: A two-line verbatim rename in `setup-card.tsx`. Nothing else changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-layout-tab-foundations/01-UI-SPEC.md

@src/components/planner/setup-card.tsx

<verbatim_copy>
<!-- These strings are the FUEL-01 / FUEL-02 contract. Copy them VERBATIM. -->
<!-- Bottles header (FUEL-01):  "Available bottles" -->
<!-- Fuel header (FUEL-02):     "Fuel selections" -->
<!-- Note: lower-case "b" in "bottles" and lower-case "s" in "selections" — match REQUIREMENTS.md exactly. -->
</verbatim_copy>

<interfaces>
<!-- The SetupCard component contract is unchanged by this plan: -->
<!-- props (extracted from the file's existing interface): -->
```typescript
interface SetupCardProps {
  bottleCounts: BottleInventory;
  selectedBottleCounts: BottleInventory;
  drinkMixes: Product[];
  solidProducts: Product[];
  selectedDrinkMixId: string | null;
  selectedSolidIds: string[];
  onBottleCountChange: (size: BottleSize, count: number) => void;
  onDrinkMixChange: (id: string | null) => void;
  onSolidChange: (ids: string[]) => void;
  variant?: 'card' | 'embedded';
}
```
<!-- This plan does not change the prop signature. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename bottles-collapsible header from "Bottles" to "Available bottles"</name>
  <files>src/components/planner/setup-card.tsx</files>
  <read_first>
    - src/components/planner/setup-card.tsx (lines 169–178 — the bottles CollapsibleTrigger block)
    - .planning/REQUIREMENTS.md (verbatim copy contract — FUEL-01)
  </read_first>
  <action>
Edit `src/components/planner/setup-card.tsx` line 171 — rename the section title displayed in the bottles `<CollapsibleTrigger>`.

old_string (line 171, exact whitespace; the surrounding `<CollapsibleTrigger>` and trailing `<p className="mt-1 ...">` lines stay unchanged):
```tsx
              <p className="section-title text-base">Bottles</p>
```

new_string:
```tsx
              <p className="section-title text-base">Available bottles</p>
```

Notes for the executor:
- Do NOT modify the helper-text line directly below (line 172–176) — `'Select bottles'` and `'X selected'` are dynamic state copy and are out of scope for this rename.
- Do NOT modify the `BottleSizeCounter` component, the inventory empty state copy ("No bottles in inventory."), or the `aria-label`s ("Add one Xml bottle" / "Remove one Xml bottle"). These are unrelated copy strings.
- Only this single line changes in this task.
  </action>
  <verify>
    <automated>
      cd /Users/ian/Desktop/Projects/Domestique && \
      grep -c '"section-title text-base">Available bottles<' src/components/planner/setup-card.tsx | grep -qx 1 && \
      grep -c '"section-title text-base">Bottles<' src/components/planner/setup-card.tsx | grep -qx 0
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"section-title text-base">Available bottles<' src/components/planner/setup-card.tsx` returns 1.
    - `grep -c '"section-title text-base">Bottles<' src/components/planner/setup-card.tsx` returns 0 (old title gone).
    - The line count of `setup-card.tsx` is unchanged (302 lines — pure copy edit).
  </acceptance_criteria>
  <done>
    Bottles section header in the setup flow now reads "Available bottles" verbatim per FUEL-01.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename fuel-selection-collapsible header from "Fuel" to "Fuel selections"</name>
  <files>src/components/planner/setup-card.tsx</files>
  <read_first>
    - src/components/planner/setup-card.tsx (lines 209–215 — the fuel CollapsibleTrigger block)
    - .planning/REQUIREMENTS.md (verbatim copy contract — FUEL-02)
  </read_first>
  <action>
Edit `src/components/planner/setup-card.tsx` line 211 — rename the section title displayed in the fuel `<CollapsibleTrigger>`.

old_string (line 211, exact whitespace; the surrounding `<CollapsibleTrigger>` and trailing `<p className="mt-1 truncate ...">` summary lines stay unchanged):
```tsx
              <p className="section-title text-base">Fuel</p>
```

new_string:
```tsx
              <p className="section-title text-base">Fuel selections</p>
```

Notes for the executor:
- Do NOT modify the inner `<h3>Drink mix</h3>` (line 220) or `<h3>Solids</h3>` (line 253) sub-headers — these are sub-section labels inside the fuel collapsible and are out of scope for FUEL-02 (which targets only the outer collapsible header).
- Do NOT modify the `getFuelSummary` helper function — its singular/plural handling lives there and is unrelated to this rename.
- Do NOT modify the page-level "Fuel plan" PageIntro title in `src/pages/planner.tsx` — that string is not in scope for this requirement.
- Only this single line changes in this task.
  </action>
  <verify>
    <automated>
      cd /Users/ian/Desktop/Projects/Domestique && \
      grep -c '"section-title text-base">Fuel selections<' src/components/planner/setup-card.tsx | grep -qx 1 && \
      grep -c '"section-title text-base">Fuel<' src/components/planner/setup-card.tsx | grep -qx 0 && \
      npm run lint --silent && \
      npm run build --silent
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"section-title text-base">Fuel selections<' src/components/planner/setup-card.tsx` returns 1.
    - `grep -c '"section-title text-base">Fuel<' src/components/planner/setup-card.tsx` returns 0 (old title gone).
    - `<h3>Drink mix</h3>` and `<h3>Solids</h3>` sub-headers are untouched (`grep -c '<h3 className="text-sm font-semibold text-ink-900">Drink mix</h3>' src/components/planner/setup-card.tsx` returns 1 and `grep -c '<h3 className="text-sm font-semibold text-ink-900">Solids</h3>' src/components/planner/setup-card.tsx` returns 1).
    - `npm run lint` exits 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>
    Fuel section header in the setup flow now reads "Fuel selections" verbatim per FUEL-02; lint and build are green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure two-line display-string rename. No input, network, auth, or persistence surface touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | I (Information Disclosure) | Stale string references in tests/snapshots | mitigate | Tree-wide grep verification (`grep -rc '"section-title text-base">Bottles<' src/` and `grep -rc '"section-title text-base">Fuel<' src/` both return 0 after edits) catches any stale imports of these labels. |
| T-02-07 | T (Tampering) | Telemetry/analytics tied to old labels | accept | n/a — these labels are display-only React text, not analytics keys. App has no analytics layer keyed on copy. |
| T-02-08 | D (Denial of Service) | Visual regression from rename | accept | Pure text rename; container, padding, typography, and Collapsible behavior are unchanged. Phase 1 no-shift contract continues to hold. |
| T-02-09 | I (Information Disclosure) | Translation/i18n keys | accept | n/a — app is English-only; no i18n layer in src/. |
</threat_model>

<verification>
Phase-level checks (run from repo root after both tasks land):
1. `npm run lint` → exit 0.
2. `npm run build` → exit 0.
3. `grep -c '"section-title text-base">Available bottles<' src/components/planner/setup-card.tsx` → 1.
4. `grep -c '"section-title text-base">Fuel selections<' src/components/planner/setup-card.tsx` → 1.
5. `grep -rc '"section-title text-base">Bottles<' src/` → 0 (old bottles label gone tree-wide).
6. `grep -rc '"section-title text-base">Fuel<' src/` → 0 (old fuel label gone tree-wide).
</verification>

<success_criteria>
- ROADMAP success criterion 2: Bottle quantity is captured exactly once, in the setup flow, under the label "Available bottles."
- ROADMAP success criterion 3: The setup flow's fuel-selection label reads "Fuel selections" (not "Fuel").
- `npm run lint` and `npm run build` both exit 0.
- All grep checks in <verification> pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-fuel-plan-cleanup/02-02-SUMMARY.md` per the GSD summary template.
</output>

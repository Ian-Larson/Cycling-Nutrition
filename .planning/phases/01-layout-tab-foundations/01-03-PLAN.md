---
id: 01-03
phase: 1
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/components/gear/gear-tabs.tsx
  - src/components/gear/gear-tab-ids.ts
autonomous: false
requirements:
  - LAYOUT-02
must_haves:
  truths:
    - "The hand-rolled GearTabs component is deleted from the codebase — no second tab style anywhere"
    - "The gear-tab-ids helper module is deleted — its responsibilities are absorbed by the canonical Tabs primitive's internal id generation"
    - "Zero source files import from @/components/gear/gear-tabs or @/components/gear/gear-tab-ids"
    - "Lint and production build remain green after deletion (no broken imports, no dead exports)"
    - "Switching between primary tabs (Fuel Plan ↔ Garage ↔ Account) and between in-page tabs at 375 / 768 / 1280px produces no horizontal layout shift — the leftmost pixel of .page-title stays put (UI-SPEC §Foundation 4 acceptance test)"
  artifacts:
    - path: "src/components/gear/gear-tabs.tsx"
      provides: "DELETED — file no longer exists"
      deletion: true
    - path: "src/components/gear/gear-tab-ids.ts"
      provides: "DELETED — file no longer exists"
      deletion: true
  key_links:
    - from: "src/pages/gear.tsx"
      to: "@/components/ui (canonical Tabs family)"
      via: "sole tab implementation in the Garage surface"
      pattern: "<Tabs"
    - from: "src/components/gear/"
      to: "(no tab-related files)"
      via: "directory listing"
      pattern: "^(?!.*gear-tabs?).*$"
---

<objective>
Complete LAYOUT-02 by deleting the now-orphaned files `src/components/gear/gear-tabs.tsx` and `src/components/gear/gear-tab-ids.ts`. After Plan 02 (Wave 2), these files have zero importers — the only consumer (`src/pages/gear.tsx`) was migrated to the canonical Tabs primitive. Plan 03 verifies the orphan status with a pre-deletion grep, deletes both files, and runs the final regression gates: lint, build, and the manual no-shift acceptance test from UI-SPEC §Foundation 4.

This plan also includes the human-verify checkpoint that closes out Phase 1 — the no-shift verification at 375 / 768 / 1280px is structurally manual (visual confirmation across breakpoints) and cannot be automated within this milestone's scope. UI-SPEC §"Recommendations Carried Forward" notes a Playwright assertion as a future enhancement; for Phase 1 the manual gate is acceptable.

Purpose: Eliminate the second tab style from the codebase entirely (UI-SPEC §Foundation 5: "Delete `gear-tabs.tsx`. ... `gear-tab-ids.ts` becomes unused — delete it"). After this plan, the codebase has exactly one in-page tab primitive (`src/components/ui/tabs.tsx`), exactly one mutually-exclusive setting toggle (`src/components/ui/segmented-control.tsx`), and exactly one route-nav pattern (`src/components/gear/gear-sub-nav.tsx`).

Output: Two deleted files. Zero source changes elsewhere. Phase 1 done.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-layout-tab-foundations/01-UI-SPEC.md
@.planning/phases/01-layout-tab-foundations/01-02-SUMMARY.md
@CLAUDE.md
@src/components/gear/gear-tabs.tsx
@src/components/gear/gear-tab-ids.ts

<interfaces>
<!-- Files targeted for deletion. After Plan 02, both are orphaned. -->

src/components/gear/gear-tabs.tsx exports:
- `function GearTabs({ value, onChange })` — replaced by canonical `<Tabs>`/`<TabList>`/`<Tab>` in Plan 02

src/components/gear/gear-tab-ids.ts exports:
- `type GearTabValue` — moved inline to src/pages/gear.tsx in Plan 02
- `function gearTabId(id)` — replaced by canonical Tabs' internal id derivation
- `function gearPanelId(id)` — replaced by canonical Tabs' internal id derivation
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify orphan status of gear-tabs.tsx and gear-tab-ids.ts</name>
  <files>(read-only verification)</files>
  <read_first>
    - .planning/phases/01-layout-tab-foundations/01-02-SUMMARY.md (confirm Plan 02 landed)
    - src/components/gear/gear-tabs.tsx (confirm still present, contents unchanged from before Plan 02)
    - src/components/gear/gear-tab-ids.ts (confirm still present)
  </read_first>
  <action>
    Run pre-deletion grep checks across the entire `src/` tree. ANY non-zero count blocks deletion. The expected counts after Plan 02:
    - `grep -rn "gear-tabs" src/` returns ONLY matches in `src/components/gear/gear-tabs.tsx` itself (the file's own filename — no importers).
    - `grep -rn "gear-tab-ids" src/` returns ONLY matches in `src/components/gear/gear-tabs.tsx` (the import statement on line 3) — once `gear-tabs.tsx` is deleted in Task 2, there will be zero matches anywhere.
    - `grep -rn "GearTabs\b" src/` returns ONLY matches inside `src/components/gear/gear-tabs.tsx` itself (the component declaration).
    - `grep -rn "gearTabId\|gearPanelId" src/` returns ONLY matches inside `src/components/gear/gear-tabs.tsx` and `src/components/gear/gear-tab-ids.ts`.

    If ANY grep returns a match in `src/pages/`, `src/store/`, `src/lib/`, `src/hooks/`, or any `src/components/` file other than the two targeted files, STOP and surface the findings — Plan 02 was incomplete. Do not delete.

    If all checks pass, proceed to Task 2.
  </action>
  <verify>
    <automated>
      bash -c '
        set -e
        # Only the targeted files should reference these symbols.
        OUTSIDE=$(grep -rln "GearTabs\|gearTabId\|gearPanelId\|gear-tab-ids\|@/components/gear/gear-tabs" src/ | grep -v "^src/components/gear/gear-tabs.tsx$" | grep -v "^src/components/gear/gear-tab-ids.ts$" || true)
        if [ -n "$OUTSIDE" ]; then
          echo "FAIL: orphan check found references outside targeted files:"
          echo "$OUTSIDE"
          exit 1
        fi
        echo "PASS: no external importers"
      '
    </automated>
  </verify>
  <acceptance_criteria>
    - The `OUTSIDE` variable in the verify command is empty (no source file outside the two targeted files references `GearTabs`, `gearTabId`, `gearPanelId`, `gear-tab-ids`, or `@/components/gear/gear-tabs`).
    - `grep -c "GearTabs" src/pages/gear.tsx` returns 0
    - `grep -c "gear-tab-ids" src/pages/gear.tsx` returns 0
    - `grep -c "@/components/gear/gear-tabs" src/pages/gear.tsx` returns 0
    - The two targeted files (`src/components/gear/gear-tabs.tsx` and `src/components/gear/gear-tab-ids.ts`) still exist (this task is verify-only, not delete).
  </acceptance_criteria>
  <done>
    Confirmed: no source file outside the two targeted files imports or references the deprecated tab primitive. Safe to proceed with deletion.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete gear-tabs.tsx and gear-tab-ids.ts</name>
  <files>
    src/components/gear/gear-tabs.tsx (deleted),
    src/components/gear/gear-tab-ids.ts (deleted)
  </files>
  <read_first>
    - .planning/phases/01-layout-tab-foundations/01-UI-SPEC.md (§Foundation 5 — confirms both files are deletion targets)
  </read_first>
  <action>
    Use `git rm` (not plain `rm`) so the deletions are staged for the next commit. Run:

    ```bash
    git rm src/components/gear/gear-tabs.tsx src/components/gear/gear-tab-ids.ts
    ```

    Both files must be removed in the same task so the working tree never has a half-orphaned state. After deletion, both files MUST NOT exist on disk.

    DO NOT delete any other file. DO NOT modify any other file. UI-SPEC §Foundation 5 explicitly preserves:
    - `src/components/ui/tabs.tsx` (the survivor)
    - `src/components/ui/segmented-control.tsx` (different role — radiogroup)
    - `src/components/gear/gear-sub-nav.tsx` (route nav, not in-page tabs)
  </action>
  <verify>
    <automated>test ! -f src/components/gear/gear-tabs.tsx &amp;&amp; test ! -f src/components/gear/gear-tab-ids.ts &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/components/gear/gear-tabs.tsx` exits 0 (file does not exist)
    - `test ! -f src/components/gear/gear-tab-ids.ts` exits 0 (file does not exist)
    - `git status --short src/components/gear/` shows both files as deleted (`D` status, staged)
    - `test -f src/components/ui/tabs.tsx` exits 0 (canonical primitive preserved)
    - `test -f src/components/ui/segmented-control.tsx` exits 0 (different-role primitive preserved)
    - `test -f src/components/gear/gear-sub-nav.tsx` exits 0 (route nav preserved)
    - `grep -rn "GearTabs\|gearTabId\|gearPanelId\|gear-tab-ids" src/` returns zero matches across the entire `src/` tree
    - `npm run lint` exits 0 (no broken imports, no unused-export warnings on dead code)
    - `npm run build` exits 0 (TypeScript module resolution succeeds; no missing-module errors)
  </acceptance_criteria>
  <done>
    Both deprecated files removed and staged. Tree-wide grep confirms zero remaining references to the deleted symbols. Lint and build both green. The codebase now has a single in-page tab primitive at `src/components/ui/tabs.tsx`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual no-shift acceptance test (UI-SPEC §Foundation 4)</name>
  <what-built>
    Phase 1 is structurally complete:
    - All 8 page wrappers strip the redundant `max-w-6xl` (Plan 01)
    - Garage in-page tabs use canonical `<Tabs>`/`<TabList>`/`<Tab>`/`<TabPanel>` (Plan 02)
    - The hand-rolled `gear-tabs.tsx` and `gear-tab-ids.ts` files are deleted (Plan 03 / Task 2)

    The four ROADMAP success criteria are now satisfied at the code level. The remaining gate is the manual visual no-shift test from UI-SPEC §Foundation 4 — this is the binding acceptance test the phase was designed around.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev`. App will be on `http://localhost:5173`.
    2. Open the app in a browser at three viewport widths in sequence. Use DevTools responsive mode (Cmd+Shift+M in Chrome / Firefox).
       - 375px (iPhone SE — mobile)
       - 768px (md breakpoint — tablet)
       - 1280px (desktop)
    3. At each viewport, perform the following navigation and observe the leftmost pixel of the page H1 (the `.page-title` heading on each route):
       a. Navigate **Fuel Plan** → note the x-coordinate of the H1's left edge.
       b. Navigate **Garage** → confirm the H1 left edge is at the SAME x-coordinate.
       c. Within Garage, click **Active setup** → **Service** → **History** in turn. Confirm the tab strip itself does not move horizontally between sub-tabs and the H1 stays put.
       d. Navigate to **Garage → Inventory** sub-route → confirm same x-coordinate.
       e. Navigate **Account** → confirm same x-coordinate.
       f. Within Fuel Plan, click **Pack** → **Ride guide** → **Stats** in turn. Confirm tab strip horizontal position is stable.
    4. Bonus check (canonical-Tabs visual parity): the Garage tab pills should render with the same orange-brand active fill, same pill shape, same height (44px on mobile, 40px desktop) as the Fuel Plan tab pills. They should look interchangeable.
    5. Bonus check (keyboard): focus the Garage tablist, press ArrowRight / ArrowLeft / Home / End — verify focus moves and the active tab updates accordingly.

    **Expected result:** No horizontal jump on any tab switch at any breakpoint. Garage and Fuel Plan tab pills are visually identical in active styling, height, and pill shape.

    **If anything shifts:** Stop and report which route, which breakpoint, and how many pixels. The most likely cause would be a missed `max-w-6xl` strip (Plan 01) or a stray width-expanding child inside a `<TabPanel>` (Plan 02 should not have introduced any, but worth checking).
  </how-to-verify>
  <acceptance_criteria>
    - Human reports "no shift" at all three breakpoints across all primary-tab transitions.
    - Human reports "no shift" within Garage Active/Service/History sub-tab transitions.
    - Human reports "no shift" within Fuel Plan Pack/Ride guide/Stats sub-tab transitions.
    - Human reports Garage tab pills render with brand-orange active fill and rounded-full pill shape (visual parity with Fuel Plan tabs).
    - Human reports keyboard navigation works in the Garage tablist (Arrow keys move focus + selection).
  </acceptance_criteria>
  <resume-signal>Type "approved" to mark Phase 1 complete, or describe issues observed (route, breakpoint, behavior) to trigger gap closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem → git index | `git rm` stages deletions atomically; rollback is `git restore --staged --worktree <files>`. |
| TypeScript module resolution | Build step verifies that no remaining `.ts/.tsx` file imports a deleted module — a missing-module error blocks the build. |
| ESLint dead-code rules | If any export from the deleted modules were still referenced, `npm run lint` would fail with `import/no-unresolved` or TS module-not-found. |
| Human → app | Manual no-shift gate is the final acceptance — depends on a human visually confirming x-coordinates do not jump. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering (broken build) | Module imports | mitigate | Task 1 is a pre-deletion grep gate that fails closed if any external importer is found. Task 2 verifies deletion AND runs `npm run build` — TypeScript module resolution catches any missed importer that grep somehow missed (e.g. dynamic `import()` strings). |
| T-03-02 | Tampering (over-deletion) | Sibling files | mitigate | Action specifies exact `git rm` command listing exactly two paths. Acceptance criteria assert the survivor files (`src/components/ui/tabs.tsx`, `src/components/ui/segmented-control.tsx`, `src/components/gear/gear-sub-nav.tsx`) still exist. |
| T-03-03 | Denial of service (visual layout shift) | All primary routes | mitigate | The manual no-shift checkpoint (Task 3) is the binding acceptance test from UI-SPEC §Foundation 4. It enumerates the exact breakpoints (375 / 768 / 1280px) and the exact sub-tab transitions to test. |
| T-03-04 | Denial of service (a11y regression) | Garage tablist | mitigate | The canonical `Tabs` primitive's keyboard contract is verified in the bonus checkpoint (ArrowRight/Left, Home, End). Roving tabindex was already verified by inspection in Plan 02's threat model. |
| T-03-05 | Repudiation | Deletion audit trail | accept | `git rm` produces a standard `D` entry in the commit; the file history is preserved in git and can be recovered via `git checkout <pre-deletion-commit> -- <path>`. Standard process, no enhanced auditing needed. |
| T-03-06 | Information disclosure | n/a | accept | Pure code deletion; no data, secrets, or PII touched. |
| T-03-07 | Spoofing | n/a | accept | No identity, auth, or session surface touched. |
| T-03-08 | Elevation of privilege | n/a | accept | No permission boundary, RLS, or role check touched. |
| T-03-09 | Tampering (focus loss) | Roving tabindex | mitigate | Manual checkpoint includes keyboard verification step. The canonical `<Tab>` sets `tabIndex={selected ? 0 : -1}` exactly as the deleted `gear-tabs.tsx` did. |

No `high` severity threats — the change is a code deletion guarded by static analysis (lint + build) and a human visual gate. The medium-severity layout/a11y threats are explicitly tested.
</threat_model>

<verification>
- `test ! -f src/components/gear/gear-tabs.tsx` exits 0
- `test ! -f src/components/gear/gear-tab-ids.ts` exits 0
- `grep -rln "GearTabs\|gearTabId\|gearPanelId\|gear-tab-ids" src/` returns no output (zero matches tree-wide)
- `npm run lint` exits 0
- `npm run build` exits 0
- Human-verify checkpoint approved: no horizontal layout shift across primary-tab and sub-tab transitions at 375 / 768 / 1280px viewport widths.
- Survivor files remain: `src/components/ui/tabs.tsx`, `src/components/ui/segmented-control.tsx`, `src/components/gear/gear-sub-nav.tsx` all present.
</verification>

<success_criteria>
- LAYOUT-02 fully satisfied: there is exactly one canonical in-page tab primitive in the codebase (`src/components/ui/tabs.tsx`); the hand-rolled secondary style is gone.
- ROADMAP success criteria 3 fully satisfied: "Every tab/segmented switcher across Fuel Plan and Garage renders from one canonical UI primitive in `src/components/ui/` — no second tab style anywhere in the app."
- ROADMAP success criteria 4 fully satisfied: "The canonical tab control behaves identically across surfaces" — verified by the human-verify checkpoint's visual-parity step.
- ROADMAP success criteria 1 fully satisfied: "Switching between primary tabs and sub-tabs produces no horizontal layout shift" — verified by the manual breakpoint sweep.
- ROADMAP success criteria 2 was satisfied by Plan 01; this plan does not regress it.
- Phase 1 done.
</success_criteria>

<output>
After completion, create `.planning/phases/01-layout-tab-foundations/01-03-SUMMARY.md` with:
- Files deleted (`src/components/gear/gear-tabs.tsx`, `src/components/gear/gear-tab-ids.ts`)
- Files preserved (`src/components/ui/tabs.tsx`, `src/components/ui/segmented-control.tsx`, `src/components/gear/gear-sub-nav.tsx`) — confirmed via test -f
- Pre-deletion grep evidence (zero external importers)
- Post-deletion tree-wide grep evidence (zero remaining references)
- Lint + build exit codes (0)
- Human-verify checkpoint outcome (approved + breakpoint coverage notes)
- Phase 1 closeout note: all four ROADMAP success criteria satisfied; Phase 2 (Fuel Plan Cleanup) and Phase 3 (Garage Cleanup) and Phase 4 (Account Consolidation) can now proceed in parallel per ROADMAP §Execution Order.
</output>

# Requirements Archive: Milestone v1.0 — Polish & Redesign Sweep

**Defined:** 2026-04-30
**Shipped:** 2026-04-30
**Archived:** 2026-05-01
**Coverage:** 13 / 13 (100%)

## v1 Requirements (all complete)

### Foundations

- [x] **LAYOUT-01** — Page-shell side padding is identical across all primary tabs (Fuel Plan, Garage, Account) and all sub-tabs within each surface. No horizontal shift when switching tabs.
  - **Outcome:** Validated. Single shared `page-shell` layout primitive sources padding for every primary route and sub-route. Manual no-shift acceptance test passed.

- [x] **LAYOUT-02** — Tab switchers across Fuel Plan and Garage use a single shared component with consistent styling, sizing, and behavior.
  - **Outcome:** Validated. All in-page tabs render from canonical `<Tabs>/<TabList>/<Tab>/<TabPanel>` in `src/components/ui/`. Hand-rolled `gear-tabs.tsx` and `gear-tab-ids.ts` deleted.

### Fuel Plan

- [x] **FUEL-01** — Remove the separate "bottles" item from the right-side Fuel Inventory rail. Bottle quantity is captured only in the setup flow, where the field is renamed to **"Available bottles"** to clarify intent.
  - **Outcome:** Validated. Right rail collapsed to one Fuel Inventory section; setup-flow label renamed.

- [x] **FUEL-02** — The setup-flow label currently shown as "Fuel" is renamed to **"Fuel selections"**.
  - **Outcome:** Validated via line rename in `src/components/planner/setup-card.tsx`.

- [x] **FUEL-03** — The right rail collapses to a single "Fuel Inventory" section — no subsections, no nested headers.
  - **Outcome:** Validated. Bottle props dropped from the rail call site; rail summary made optional.

- [x] **FUEL-04** — Quick-counter copy in Fuel Inventory uses correct singular/plural forms and "drink mix" instead of "mix".
  - **Outcome:** Validated. Counter renders forms like `2 drink mix · 1 solid`, `1 drink mix · 2 solids`, `1 solid` correctly.

### Garage

- [x] **GEAR-01** — The "Due Soon" service card renders only on the Service tab. Hidden on Active Setup and History.
  - **Outcome:** Validated. `GearDuePreviewBand` moved into the Service `TabPanel`.

- [x] **GEAR-02** — The Active Setup / Service / History tab strip stays in a fixed vertical position regardless of which tab is active.
  - **Outcome:** Validated. Tab strip pinned; "Due Soon" no longer pushes it down.

- [x] **GEAR-03** — The Garage → Inventory sub-page no longer shows the top counter cards.
  - **Outcome:** Validated. 5-card grid removed; status counts remain visible in `ChipRow` filter labels — no information lost.

- [x] **GEAR-04** — Inventory sub-page component cards use a denser layout (reusing an existing leaner card style from elsewhere in the app).
  - **Outcome:** Validated. Per-instance `<Card>`s swapped to `DividedRowList` rows. Net −85 lines (143 deletions / 58 insertions). Significantly more parts fit per scroll.

### Account

- [x] **ACCT-01** — Account section consolidates into a single 2-pane page — **Athlete information** on top, **Account / Sync / Login** as a condensed bottom section.
  - **Outcome:** Validated. `<AthletePane />` extracted into `src/components/account/`; `account.tsx` rewritten as the consolidated 2-pane page; `/athlete` and `/settings` redirect to `/account[#preferences]`. Section nav for `account` emptied.

### Docs

- [x] **DOC-01** — PRODUCT.md is rewritten to match shipped reality (orange `#f8622e` brand, no `#547597`, no dark-mode parity, grounded in `src/components/ui/` and `src/index.css` `@theme` tokens). Once accurate, PRODUCT.md is committed (was previously untracked).
  - **Outcome:** Validated. PRODUCT.md committed in `3482a04`.

- [x] **DOC-02** — CLAUDE.md font reference is updated from "Outfit, Source Sans 3" to "IBM Plex Sans".
  - **Outcome:** Validated. One-line edit at CLAUDE.md line 29 (commit `24fe36a`).

## v2 Requirements

None deferred. All v1 requirements shipped; future polish/redesign work scoped as a new milestone.

## Out of Scope (recorded)

| Feature | Reason | Status |
|---------|--------|--------|
| Dark mode | Deferred to a future milestone. PRODUCT.md's earlier "plan for both light and dark" intent was rolled back for this sweep. | Still deferred |
| Palette migration to `#547597` | Rejected. The shipped orange brand `#f8622e` stays. PRODUCT.md was rewritten to reflect this rather than the code being migrated. | Permanently rejected |
| New features (fuel-planning logic, gear capability, analyzer features) | Polish-only milestone; no functional additions. | Available for future milestones |
| Backend / Supabase changes | Cloud sync, edge functions, and Strava integration stayed as they were. | Available for future milestones |
| New UI primitives | Reuse what's already in `src/components/ui/`. | Constraint satisfied — no new primitives shipped |
| Targeted performance work | Beyond what naturally fell out of cleanup, no perf phase. | Available for future milestones |
| Project-level domain research (4-agent spawn) | `/impeccable` and its subskills provided the design lens during per-phase planning, replacing project-level research subagents. | Pattern adopted |

## Traceability (final)

| Requirement | Phase | Final Status |
|-------------|-------|--------------|
| LAYOUT-01 | Phase 1 | Complete |
| LAYOUT-02 | Phase 1 | Complete |
| FUEL-01 | Phase 2 | Complete |
| FUEL-02 | Phase 2 | Complete |
| FUEL-03 | Phase 2 | Complete |
| FUEL-04 | Phase 2 | Complete |
| GEAR-01 | Phase 3 | Complete |
| GEAR-02 | Phase 3 | Complete |
| GEAR-03 | Phase 3 | Complete |
| GEAR-04 | Phase 3 | Complete |
| ACCT-01 | Phase 4 | Complete |
| DOC-01 | Phase 5 | Complete |
| DOC-02 | Phase 5 | Complete |

**Notes:**
- No requirements changed scope mid-milestone.
- No requirements were dropped.
- No requirements were added (no decimal phases).

---
*Archived 2026-05-01 via `/gsd-complete-milestone` v1.0.*
